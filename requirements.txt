# Interface
customtkinter>=5.2.0

# Editor Web (será usado na Etapa 2)
pywebview>=4.4.0

# Processamento HTML (será usado na Etapa 2)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Geração DOCX (Etapa 3)
python-docx>=1.1.0
docxtpl>=0.16.0

# Modo Lote (Etapa 3)
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1

# Conversão DOCX para PDF
# 1. Microsoft Word COM - Melhor qualidade (requer MS Word instalado no Windows)
pywin32>=306

# 2. docx2pdf - Alta qualidade (gratuito e open source)
docx2pdf>=0.1.8

# 3. Aspose.Words - Alta qualidade (comercial com trial - opcional)
# aspose-words>=24.1.0  # Descomente se quiser usar

# Utilidades
Pillow>=10.0.0
pyperclip>=1.8.0

# Testes
pytest>=7.4.0
pytest-cov>=4.1.0
