# Interface
customtkinter>=5.2.0

# Editor Web (será usado na Etapa 2)
pywebview>=4.4.0

# Processamento HTML (será usado na Etapa 2)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Geração DOCX (Etapa 3)
python-docx>=1.1.0
docxtpl>=0.16.0

# Modo Lote (Etapa 3)
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1

# Conversão DOCX para PDF
# 1. LibreOffice - MELHOR fidelidade para bordas, linhas e negrito (requer instalação separada no Windows)
#    Download: https://www.libreoffice.org/download/
#    Caminho padrão: C:\Program Files\LibreOffice\program\soffice.exe
#    Este é o método preferido para MÁXIMA FIDELIDADE ao documento original

# 2. Microsoft Word COM - Boa qualidade (requer MS Word instalado no Windows)
pywin32>=306

# 3. Aspose.Words - Alta qualidade e compatibilidade com threads (recomendado para modo paralelo)
# Instale com: pip install aspose-words
# Versão: >=24.1.0 - Descomente a linha abaixo se quiser usar
# aspose-words>=24.1.0

# Utilidades
Pillow>=10.0.0
pyperclip>=1.8.0
platformdirs>=3.0.0

# Testes
pytest>=7.4.0
pytest-cov>=4.1.0
